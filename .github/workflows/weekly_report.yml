name: 주간 리포트

on:
  # 트리거: 외부 cron-job.org가 workflow_dispatch로 호출 (일요일 19:00 KST).
  #
  # ⚠️ GitHub Actions schedule 제거 (2026-06-08 결정):
  #   - briefing.yml과 동일 문제 — schedule이 ~10시간 지연 발화
  #     (예: 의도 일요일 09:00 KST → 실제 일요일 19:13 KST)
  #   - 의도된 시각과 실제 도착 시각의 불일치 해소 + briefing과 트리거 구조 통일
  #   - cron-job.org에 weekly 잡 추가 시 일요일 19:00 KST 정시 발화 보장
  #
  # cron-job.org 자체 장애 시 fallback:
  #   - GitHub Actions UI에서 workflow_dispatch 수동 실행 가능
  workflow_dispatch:     # 수동 실행 가능

concurrency:
  group: stock-briefing
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  report:
    runs-on: ubuntu-latest
    # GitHub Pages environment 일시 제거 (codeload 장애 대비). 복구 후 다시 추가.

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Python 3.11 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: 패키지 설치
        run: pip install -r requirements.txt

      - name: 주간 리포트 실행
        env:
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
          KAKAO_ACCESS_TOKEN: ${{ secrets.KAKAO_ACCESS_TOKEN }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
        run: python weekly_report.py

      - name: 시그널 성과 분석 캐시 갱신
        # 과거 추천 outcome → 시그널 버킷 통계를 signal_performance_cache에 저장.
        # AI 프롬프트에 참고용으로만 삽입되며, 실패해도 주간 리포트 흐름은 유지.
        continue-on-error: true
        run: python signal_performance.py

      - name: Kakao 토큰 자동 갱신 반영
        if: success() && hashFiles('/tmp/kakao_tokens_new.json') != ''
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          NEW_ACCESS=$(python3 -c "import json; print(json.load(open('/tmp/kakao_tokens_new.json'))['access_token'])")
          NEW_REFRESH=$(python3 -c "import json; d=json.load(open('/tmp/kakao_tokens_new.json')); print(d.get('refresh_token',''))")
          [ -n "$NEW_ACCESS" ] && printf '%s' "$NEW_ACCESS" | gh secret set KAKAO_ACCESS_TOKEN --repo ${{ github.repository }}
          [ -n "$NEW_REFRESH" ] && printf '%s' "$NEW_REFRESH" | gh secret set KAKAO_REFRESH_TOKEN --repo ${{ github.repository }}

      - name: DB + 대시보드(docs/) 자동 커밋
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -n "$(git status --porcelain stock_briefing.db docs/)" ]; then
            git add stock_briefing.db docs/ 2>/dev/null || git add stock_briefing.db
            git commit -m "chore: 주간 리포트 + 대시보드 갱신 ($(TZ='Asia/Seoul' date '+%Y-%m-%d %H:%M KST'))"
            git pull --rebase origin ${{ github.ref_name }} || true
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "변경 없음 — 커밋 생략"
          fi

      # GitHub Pages 설정: Settings → Pages → Source = "Deploy from a branch", Branch = main /docs.

      - name: 실행 결과 요약
        if: always()
        run: |
          echo "## 주간 리포트 실행" >> $GITHUB_STEP_SUMMARY
          echo "- 시각 (KST): $(TZ='Asia/Seoul' date)" >> $GITHUB_STEP_SUMMARY
          echo "- 대시보드: https://gezzang82.github.io/stock-briefing/" >> $GITHUB_STEP_SUMMARY

      - name: 실패 시 카카오톡 알림
        if: failure()
        env:
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
          KAKAO_ACCESS_TOKEN: ${{ secrets.KAKAO_ACCESS_TOKEN }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          export KST_TIME=$(TZ='Asia/Seoul' date '+%Y-%m-%d %H:%M')
          python3 - <<'PY'
          import os
          from kakao_sender import send_message
          msg = (
              "🚨 주간 리포트 워크플로우 실패\n"
              f"시각: {os.environ.get('KST_TIME', '?')} KST\n"
              f"트리거: {os.environ.get('EVENT_NAME', '?')}\n"
              f"로그: {os.environ.get('RUN_URL', '?')}"
          )
          send_message(msg)
          PY
